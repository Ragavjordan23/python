myset = set(["a", "b", "c"])
print(myset)

myset.add("d")
print(myset)

output:

{'c', 'b', 'a'}
{'d', 'c', 'b', 'a'}
--------
# pop()

a = {7,5,3,8,1,6,48}
a1 = a.pop()
print(a1)
print(a)

# o/p:
1
{3, 5, 6, 7, 8, 48}
